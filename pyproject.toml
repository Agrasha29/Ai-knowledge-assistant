[project]
name = "ai-knowledge-assistant"
version = "1.0.0"
description = "AI-powered PDF question answering assistant"
requires-python = ">=3.12"
dependencies = [
    "Flask",
    "flask-cors",
    "PyPDF2",
    "openai",
    "python-dotenv"
]

[tool.vercel]
entrypoint = "Backend.app:app"